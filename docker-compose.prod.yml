# docker-compose.prod.yml
# Production HTTPS contract:
# - Public traffic must terminate TLS at an external load balancer / reverse proxy
#   or at a hardened edge gateway before reaching this compose stack.
# - The edge must set X-Forwarded-Proto=https so Django can enforce
#   SECURE_SSL_REDIRECT and secure CSRF/session cookies.
# - The nginx service below is the internal HTTP origin/gateway. Do not expose
#   this port directly to the Internet without TLS termination in front of it.
x-scmd-app-env: &scmd_app_env
  DB_HOST: db
  DB_PORT: "5432"
  SQL_DATABASE: ${SQL_DATABASE}
  SQL_USER: ${SQL_USER}
  SQL_PASSWORD: ${SQL_PASSWORD}
  DATABASE_URL: ${DATABASE_URL}
  REDIS_URL: ${REDIS_URL}
  SCMD_ORGANIZATION_ID: ${SCMD_ORGANIZATION_ID}
  SCMD_DOCKER_COMPOSE: production

services:
  migrate:
    build: .
    container_name: scmd_migrate_prod
    command: ["/app/scripts/entrypoint.sh", "python", "manage.py", "migrate", "--noinput"]
    user: "1000:1000"
    env_file:
      - .env
    environment:
      <<: *scmd_app_env
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: "no"

  collectstatic:
    build: .
    container_name: scmd_collectstatic_prod
    command: ["python", "manage.py", "collectstatic", "--noinput"]
    user: "1000:1000"
    env_file:
      - .env
    environment:
      <<: *scmd_app_env
      SCMD_COLLECTSTATIC_ON_START: "0"
    volumes:
      - static_volume:/app/staticfiles
      - media_volume:/app/media
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: "no"

  # --- 1. WEB APP (DAPHNE) ---
  web:
    build: .
    container_name: scmd_web_prod
    command: ["daphne", "-b", "0.0.0.0", "-p", "8000", "config.asgi:application"]
    user: "1000:1000"
    expose:
      - "8000" # Chỉ mở nội bộ cho Nginx thấy
    volumes:
      - static_volume:/app/staticfiles
      - media_volume:/app/media
    env_file:
      - .env # Load biến môi trường từ file .env
    environment:
      <<: *scmd_app_env
      SECURE_SSL_REDIRECT: ${SECURE_SSL_REDIRECT:-True}
      SCMD_COLLECTSTATIC_ON_START: "0"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: always
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import sys, urllib.request; response = urllib.request.urlopen('http://127.0.0.1:8000/api/v1/health/', timeout=5); sys.exit(0 if response.status == 200 else 1)",
        ]
      interval: 30s
      timeout: 10s
      retries: 3

  # --- 2. NGINX (GATEWAY) ---
  nginx:
    image: nginx:1.25-alpine
    container_name: scmd_nginx
    ports:
      # Internal origin HTTP only. Production TLS must be terminated in front of
      # this compose stack and forward X-Forwarded-Proto=https.
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
      - static_volume:/app/staticfiles # Share volume để lấy file tĩnh
      - media_volume:/app/media
    depends_on:
      - web
    restart: always

  # --- 3. WORKER (CELERY) ---
  celery:
    build: .
    command: celery -A config worker -l info
    user: "1000:1000"
    env_file:
      - .env
    environment:
      <<: *scmd_app_env
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: always

  # --- 4. BEAT (SCHEDULED TASKS) ---
  celery_beat:
    build: .
    command: celery -A config beat -l info
    user: "1000:1000"
    env_file:
      - .env
    environment:
      <<: *scmd_app_env
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: always

  # --- 5. DATABASE (POSTGIS) ---
  db:
    image: postgis/postgis:15-3.3
    container_name: scmd_db_prod
    environment:
      POSTGRES_DB: ${SQL_DATABASE}
      POSTGRES_USER: ${SQL_USER}
      POSTGRES_PASSWORD: ${SQL_PASSWORD}
    volumes:
      - postgres_data_prod:/var/lib/postgresql/data
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 5

  # --- 6. REDIS ---
  redis:
    image: redis:7-alpine
    container_name: scmd_redis_prod
    restart: always
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3

volumes:
  postgres_data_prod:
  static_volume:
  media_volume:
